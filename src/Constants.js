const STATUS_KEY = 'STATUS_KEY';
const TARGET_DATE = 'targetDate';

const STATUS_WAITING = 'waiting_key';
const STATUS_EXPIRED = 'expired_key';
const STATUS_COUTING = 'couting_key';
const STATUS_SUCCESS = 'success_key';

module.exports = {
    STATUS_KEY,
    TARGET_DATE,
    STATUS_WAITING,
    STATUS_EXPIRED,
    STATUS_COUTING,
    STATUS_SUCCESS
}